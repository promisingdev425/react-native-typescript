# Example schema until we can get the real schema
type ManagementCompany {
  id: Int!
  name: String
  properties: [Property]
}

type Property {
  id: Int!
  name: String
  address: String
}

type Query {
  properties: [Property]
  managementCompany(id: Int!): ManagementCompany
}

