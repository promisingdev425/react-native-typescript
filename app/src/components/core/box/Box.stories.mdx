import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from '@storybook/addon-docs'

import { Box } from './Box'

<Meta title="Components/Box" argTypes={{}} />

# Box

<Description of={Box} />

export const Template = (props) => <Box {...props} />

<Canvas isColumn>
  <Story name="Pink box" args={{
    bg: 'brandPink',
    p: 5,
    br: 5,
  }}>
    {Template.bind({})}
  </Story>
  <Story name="Orange box" args={{
    bg: 'brandOrange',
    p: 5,
    br: 5,
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Box} />
