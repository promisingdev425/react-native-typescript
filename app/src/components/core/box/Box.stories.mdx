import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';

import { Box } from './Box.jsx';

<Meta
  title="Components/Box"
  argTypes={{
  }}
/>

# Box

<Description of={ Box } />

export const Template = props => <Box {...props} />;

<Canvas>
  <Story name="Pink colored Box" args={{
    bg: 'brandPink',
    p:5,
    borderRadius: 5,
  }}>
    {Template.bind({})}
  </Story>

  <Story name="Orange colored Box" args={{
    bg: 'brandOrange',
    p:5,
    borderRadius: 5,
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={ Box } />
