import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from '@storybook/addon-docs'
import { userPlaceholder } from '~/assets/images'

import { themes } from '~/theme'
import { Avatar } from './Avatar'

<Meta
  title="Components/Avatar"
  argTypes={{
    size: {
      options: ['xs', 'sm', 'md', 'lg'],
      control: { type: 'select' },
    },
    bg: {
      options: Object.keys(themes.light.colors),
      control: { type: 'select' },
    },
  }}
/>

# Avatar

<Description of={Avatar} />

export const Template = (props) => <Avatar {...props} />

<Canvas>
  <Story
    name="Avatar"
    args={{
      image: 'https://avatars.githubusercontent.com/u/20338216?v=4',
      size: 'sm',
      name: 'John Doe',
      bg: 'lightGray',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Avatar} />
