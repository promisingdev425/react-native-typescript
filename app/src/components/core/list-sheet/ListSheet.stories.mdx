import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs'
import { View } from 'react-native'

import { Icons } from '~/assets'

import { ListSheet } from './ListSheet'

<Meta
  title="Components/ListSheet"
  argTypes={{
    onChange: { action: 'onChange' },
    onChangeAfterHide: { action: 'onChange' }
  }}
/>

# ListSheet

<Description of={ ListSheet } />

export const Template = props => <ListSheet {...props} />

<Canvas>
  <Story
    name="ListSheet"
    args={{
      open: true,
      title: 'Leasing Reports',
      options: [
        {
          Icon: Icons.Application,
          value: 'application',
          label: 'Engagement Score',
        },
        {
          Icon: Icons.Application,
          value: 'responsiveness',
          label: 'Responsiveness',
        },
      ],
      height: 48,
      isHideAfterChoose: true,
    }}
    decorators={[
      (sb) => <View style={{height: 300}}>{sb()}</View>
    ]}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={ ListSheet } />
