import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from '@storybook/addon-docs'

import { Title } from './Title'

<Meta title="Components/Title" />

# Title

<Description of={Title} />

export const Template = (props) => <Title {...props} />

<Canvas isColumn>
  <Story
    name="Navigation Title"
    args={{
      variant: 'navigation',
      children: 'This is the [navigation] title',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Success colored Header Title"
    args={{
      variant: 'header',
      color: 'positive',
      children: 'This is the [header] title',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Warning colored Section Title"
    args={{
      variant: 'section',
      color: 'warning',
      children: 'This is the [section] title',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Negative colored Body Title"
    args={{
      variant: 'body',
      color: 'negative',
      children: 'This is the [body] title',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Title} />
