import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';

import { Title } from './Title.jsx';

<Meta
  title="Components/Title"
  argTypes={{
  }}
/>

# Title

<Description of={ Title } />

export const Template = props => <Title {...props} />;

<Canvas>
  <Story name="Navigation Title" args={{
    variant: 'navigation',
  }}>
    {Template.bind({})}
  </Story>

  <Story name="Success colored Header Title" args={{
    variant: 'header',
    color: 'positive',
  }}>
    {Template.bind({})}
  </Story>

  <Story name="Warning colored Section Title" args={{
    variant: 'section',
    color: 'warning',
  }}>
    {Template.bind({})}
  </Story>

  <Story name="Negative colored Body Title" args={{
    variant: 'body',
    color: 'negative',
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={ Title } />
