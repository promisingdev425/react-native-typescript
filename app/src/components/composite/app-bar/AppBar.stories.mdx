import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from '@storybook/addon-docs'
import { PropertyIcon } from '~/assets/images'

import { AppBar } from './AppBar'

<Meta title="Components/AppBar" argTypes={{}} />

# AppBar

<Description of={AppBar} />

export const Template = (props) => <AppBar {...props} />

<Canvas isColumn>
  <Story name="AppBar with left and right icons" args={{
    title: "Appbar header",
    leftItem: <PropertyIcon />,
    rightItem: <PropertyIcon />
  }}>
    {Template.bind({})}
  </Story>
  <Story name="AppBar with title and left icon" args={{
    title: "Appbar header",
    leftItem: <PropertyIcon />
  }}>
    {Template.bind({})}
  </Story>
  <Story name="AppBar with title only" args={{
    title: "Header title",
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={AppBar} />
