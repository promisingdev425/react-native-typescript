import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  Description,
} from '@storybook/addon-docs'

import { AppBar } from './AppBar.jsx'

<Meta title="Components/AppBar" argTypes={{}} />

# AppBar

<Description of={AppBar} />

export const Template = (props) => <AppBar {...props} />

<Canvas>
  <Story name="AppBar" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={AppBar} />
