import { Meta, Story, Canvas, ArgsTable, Description } from '@storybook/addon-docs';

import { ToggleGroup } from './ToggleGroup';

<Meta
  title="Components/ToggleGroup"
  argTypes={{
  }}
/>

# ToggleGroup

<Description of={ ToggleGroup } />

export const Template = props => <ToggleGroup {...props} />;

<Canvas>
  <Story
    name="ToggleGroup"
    args={{
      height: 32,
      options: [
        { id: 'w', value: 'week', label: 'W', accessibilityLabel: 'Press Week' },
        { id: 'm', value: 'month', label: 'M', accessibilityLabel: 'Press Month' },
        { id: 'q', value: 'quater', label: 'Q', accessibilityLabel: 'Press Quater' },
        { id: 'y', value: 'year', label: 'Y', accessibilityLabel: 'Press Year' },
      ],
      onChange: ({ value }) => console.log('click', value)
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={ ToggleGroup } />
