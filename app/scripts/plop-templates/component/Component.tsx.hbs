import React from 'react'
import { View } from 'react-native'

export interface I{{pascalCase name}} {
}

/**
 * `<{{pascalCase name}}>`
 *
 * @param {object} props
 * @return {React.ReactNode}
 */
const {{pascalCase name}}: React.FC<I{{pascalCase name}}> = ({
  ...rest
}) => {
  return (
    <View {...rest} >
    </View>
  )
}
